import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ma.jumatik.android',
  appName: 'Jumatik',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
